# Lightweight AIRS GNN preset for fast CI/smoke runs
defaults:
  - base

hidden_dim: 128
out_dim: 128
n_layers: 2
heads: 2
dropout: 0.05

positional_encoding:
  use: false

edge_features:
  use: true
  dims: 3  # drop seam_flag for speed if desired