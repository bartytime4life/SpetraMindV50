# =====================================================================
# SpectraMind V50 — Diagnostics Dashboard Configuration
# ---------------------------------------------------------------------
# Purpose:
#   Controls which diagnostics appear in the HTML dashboard.
#   Versioned output with embedded plots, overlays, and metadata.
# =====================================================================

defaults:
  - base

diagnostics:
  dashboard:
    include_fft: true
    include_umap: true
    include_shap: true
    include_symbolic: true
    include_calibration: true
    include_cli_log_summary: true
    versioned_output: true
